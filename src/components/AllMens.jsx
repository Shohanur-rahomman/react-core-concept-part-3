import { use } from "react";


const AllMens = async ({ allData }) => {
    const maneDetails = use(allData)
    console.log(maneDetails);
    
    return (
        <div>
           
        </div>
    );
};

export default AllMens;